# Command (Applet) List

The command list is now maintained in a single place — the project
[README](../../../README.md#commands) — and generated from the registered
applets by `make command-list`, so it never drifts from the binary.

You can also run `mimixbox --list` to print the list on the terminal.
