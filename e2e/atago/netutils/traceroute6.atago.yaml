# Migrated from mimixbox's ShellSpec test/it/spec/traceroute6_spec.sh (no helper; inline).
# Universal --help contract (Issue #477): exit 0, "Usage: traceroute6" on stdout,
# empty stderr. Privileged/networked/destructive applet exercised via --help only.
version: "1"

suite:
  name: mimixbox traceroute6

scenarios:
  - name: describes itself with --help
    steps:
      - run:
          command: traceroute6 --help
      - assert:
          exit_code: 0
          stdout:
            contains: "Usage: traceroute6"
          stderr:
            empty: true
