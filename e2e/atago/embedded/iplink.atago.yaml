# Migrated from mimixbox's ShellSpec test/it/spec/iplink_spec.sh.
# Universal --help contract (Issue #477): exit 0, "Usage: iplink" on stdout,
# empty stderr. Privileged/networked/destructive applets are exercised via
# --help only so the suite never reboots, formats, loads modules, or touches
# the network.
version: "1"

suite:
  name: mimixbox iplink

scenarios:
  - name: describes itself with --help
    steps:
      - run:
          command: iplink --help
      - assert:
          exit_code: 0
          stdout:
            contains: "Usage: iplink"
          stderr:
            empty: true
