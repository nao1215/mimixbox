# Migrated from mimixbox's ShellSpec test/it/spec/ping6_spec.sh.
# Universal --help contract (Issue #477): exit 0, "Usage: ping6" on stdout,
# empty stderr. Privileged/networked/destructive applets are exercised via
# --help only so the suite never reboots, formats, loads modules, or touches
# the network.
version: "1"

suite:
  name: mimixbox ping6

scenarios:
  - name: describes itself with --help
    steps:
      - run:
          command: ping6 --help
      - assert:
          exit_code: 0
          stdout:
            contains: "Usage: ping6"
          stderr:
            empty: true
