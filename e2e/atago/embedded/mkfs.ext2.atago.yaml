# Migrated from mimixbox's ShellSpec test/it/spec/mkfs.ext2_spec.sh.
# Universal --help contract (Issue #477): exit 0, "Usage: mkfs.ext2" on stdout,
# empty stderr. Privileged/networked/destructive applets are exercised via
# --help only so the suite never reboots, formats, loads modules, or touches
# the network.
version: "1"

suite:
  name: mimixbox mkfs.ext2

scenarios:
  - name: describes itself with --help
    steps:
      - run:
          command: mkfs.ext2 --help
      - assert:
          exit_code: 0
          stdout:
            contains: "Usage: mkfs.ext2"
          stderr:
            empty: true
