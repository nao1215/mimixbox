# Migrated from mimixbox's ShellSpec test/it/spec/ash_spec.sh (no helper; inline).
# ash is a compatibility front-end over MimixBox's shell (mbsh). Universal
# --help contract (Issue #477) plus a purpose-paragraph check.
version: "1"

suite:
  name: mimixbox ash

scenarios:
  - name: describes itself with --help
    steps:
      - run:
          command: ash --help
      - assert:
          exit_code: 0
          stdout:
            contains: "Usage: ash"
          stderr:
            empty: true

  - name: documents its purpose in --help
    steps:
      - run:
          command: ash --help
      - assert:
          exit_code: 0
          stdout:
            contains: "compatibility front-end over MimixBox"
